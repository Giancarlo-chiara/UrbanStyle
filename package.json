{
  "name": "urbanstyle",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "UrbanStyle — tienda de moda urbana. Frontend React + API Node/Express/Prisma sobre PostgreSQL.",
  "scripts": {
    "build": "prisma generate && prisma migrate deploy && node backend-node/cargar-seed.mjs --si-vacia && npm --prefix ecommerce install && npm --prefix ecommerce run build",
    "db:migrate": "prisma migrate deploy",
    "db:studio": "prisma studio"
  },
  "prisma": {
    "schema": "backend-node/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "prisma": "^6.19.3"
  }
}
