# Producción (Vercel): la API vive en el MISMO dominio que el frontend, así que
# basta una ruta relativa. Esto es lo que hace que no haga falta CORS.
#
# No es un secreto: se versiona a propósito para que el build de Vercel lo tome
# sin configurar nada en el panel.
VITE_API_URL=/api
